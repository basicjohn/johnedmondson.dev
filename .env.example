# Contact form API (API Gateway). The value is public at runtime — treat
# secrets differently. Rotate / throttle the endpoint if abused.
VITE_CONTACT_API_URL=https://9t0tc13wwe.execute-api.us-east-2.amazonaws.com/staging
